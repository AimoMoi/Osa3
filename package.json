{
  "name": "puhelinluettelo3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rmdir /S /Q dist && cd ../phonebook && npm run build && xcopy dist ..\\puhelinluettelo3 /E /I",
    "deploy:full": "npm run build:ui && git add . && git commit -m 'uibuild' && git push",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
